=== ИСПОЛЬЗОВАНИЕ НА РАЗНЫХ ОС ===
Скрипт поддерживает Windows, Linux и macOS.

• Windows 10/11:
  — Запуск: двойной клик по main.py или через ярлык (Whisper Fast GUI.lnk).
  — Без консоли: run_whisper.vbs.
  — Установка зависимостей: install.bat (или кнопка [Зависимости] в программе).
  — Клик по ссылке в логе: открытие файла; Shift+клик — открыть папку и выделить файл в проводнике.
  — Звук завершения: при отсутствии .mp3 в папке используется системный звук Windows.

• Linux:
  — Запуск: из каталога проекта выполнить: python3 main.py (или python main.py).
  — Для Drag & Drop и отображения нужен рабочий стол с поддержкой X11/Wayland и tkinter.
  — Установка: установите Python 3, FFmpeg и зависимости (через pip или кнопку [Зависимости]).
  — Открытие файлов/папок из лога: используется xdg-open (пакет xdg-utils).
  — Звук завершения: при отсутствии .mp3 в папке — попытка воспроизвести системный звук (freedesktop); иначе звук не воспроизводится.

• macOS:
  — Запуск: python3 main.py из каталога проекта.
  — Открытие файлов из лога: open (обычный клик) и open -R (Shift+клик — показать в Finder).
  — Звук завершения: как на Linux (системные звуки при отсутствии .mp3).

На всех ОС требуются: Python 3.9+, FFmpeg в PATH, установленные зависимости (faster-whisper, torch, pydub и др.). Рекомендуется NVIDIA GPU с CUDA для ускорения на Windows/Linux.

=== ОБ ОБОРУДОВАНИИ И СКРИПТЕ ===
Скрипт основан на технологии OpenAI Whisper и библиотеке Faster-Whisper.
Используется модель "large-v3-turbo" для высокой точности и скорости.
Версия и дата приложения отображаются в верхней части окна (справа от кнопок).

=== СТРУКТУРА ПРОЕКТА ===
WhisperFastGUI/
├── main.py              — точка входа: проверка зависимостей, иконка панели задач, запуск GUI
├── gui.py                — главное окно: очередь, настройки, транскрибация, лог
├── model_manager.py      — загрузка и выгрузка модели Whisper (Singleton)
├── input_files.py        — добавление файлов/каталогов, валидация, Drag & Drop
├── lang_manager.py       — переводы интерфейса (EN/UK/RU), загрузка lang.json
├── i18n_fallback.py      — резервные функции t/set_language при недоступности lang_manager
├── config.py             — константы: версия, форматы, модель, языки
├── utils.py              — форматирование времени, звук завершения
├── installer.py          — установка/обновление зависимостей, проверка системы
├── lang.json             — тексты интерфейса на трёх языках
├── settings.json         — збережені налаштування: мова інтерфейсу, каталоги, слідкування, пристрій, опції (створюється при першому збереженні)
├── Readmy.txt            — эта справка
├── favicon.ico           — иконка окна и панели задач
├── install.bat           — первичная установка зависимостей
├── run_whisper.vbs         — запуск приложения (без окна консоли)
└── __pycache__/          — кэш Python (создаётся автоматически)

Опционально в папке: .mp3 для звука завершения.

=== ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ ===
• Python: 3.9 или выше (рекомендуется 3.10+). Поддерживается Python 3.13+.
• ОС: Windows 10/11, Linux (X11/Wayland), macOS. Подробнее — в начале файла (Использование на разных ОС).
• GPU: Рекомендуется видеокарта NVIDIA с поддержкой CUDA 12.1.
• Важно: AMD Radeon и другие не-CUDA GPU работают только в режиме CPU (без ускорения).
• FFmpeg: Требуется в PATH для обработки медиафайлов.
• Python 3.13+: Автоматически устанавливается pyaudioop (замена удалённого audioop).

=== ПОДДЕРЖИВАЕМЫЕ ФОРМАТЫ ===
Аудио: .mp3, .wav, .m4a, .flac, .ogg
Видео: .mp4, .mkv, .avi, .mov
Требуется FFmpeg в PATH для декодирования медиафайлов.

=== ФУНКЦИОНАЛ ===
Преобразует речь из аудио и видео в текст.
Выходные файлы создаются в папке сохранения или рядом с исходным файлом:

1. .txt — чистый текст транскрибации (каждый сегмент на новой строке).
2. .srt — субтитры: нумерация сегментов, временные метки HH:MM:SS,mmm --> HH:MM:SS,mmm, текст сегмента.

Опционально (при включённой опции "Сохранить извлечённое аудио (MP3)"):
3. _audio.mp3 — извлечённая аудиодорожка (удобно для видео или архива аудио рядом с субтитрами).

=== РЕЖИМЫ ОБРАБОТКИ ===
• Один файл в очереди: Выберите файл и нажмите [Старт] — диалог не показывается, обработка начинается сразу.
• Несколько файлов, выбран один: При нажатии [Старт] появляется диалог: "Только выбранный файл", "Все файлы в очереди" или "Отмена".
• Только новые: Обрабатываются только файлы без пометки "– обработан".
• Вся очередь: Обработка всех файлов по порядку.

=== НАСТРОЙКИ УСТРОЙСТВА И ВЫВОДА ===
• AUTO — автоматический выбор: при наличии совместимой NVIDIA GPU используется GPU, иначе CPU.
• GPU — принудительно видеокарта (если CUDA доступна). Для старых карт (например, GTX 1060) — безопасный режим int8.
• CPU — принудительно процессор, int8 (подходят также AMD Radeon и другие не-CUDA GPU).
Модель загружается один раз и используется для всех файлов (Singleton, экономия памяти).

Дополнительные опции:
• Воспроизвести звук по завершении очереди — звуковое уведомление после обработки всех файлов.
• Сохранить извлечённое аудио (MP3) — для каждого файла сохраняется <имя>_audio.mp3 (извлечённая дорожка).

=== ЯЗЫКИ ИНТЕРФЕЙСА ===
• EN — английский (по умолчанию), UK — украинский, RU — русский.
• Переключатель (🌐 EN UK RU) в верхней части окна. При запуску та закритті програми в settings.json зберігаються: мова інтерфейсу, каталог збереження, шлях та прапорець «Слідкування за каталогом», пристрій (AUTO/GPU/CPU), опції «Відтворити звук» та «Зберегти Mp3».
• Язык интерфейса не влияет на распознавание речи. Язык распознавания задаётся переключателем "Переключатель языка" (AUTO, RU, UK, EN).

=== ОПИСАНИЕ КНОПОК ===
• [Старт] — запуск обработки (с выбором режима).
• [Отмена] — остановка текущей задачи (корректное завершение текущего сегмента).
• [Добавить файлы] — выбор одного или нескольких файлов.
• [Добавить каталог] — добавление всех поддерживаемых файлов из каталога рекурсивно.
• [Очистить очередь] — удаление всех файлов из списка.
• [Система] — проверка GPU и FFmpeg.
• [Зависимости] — установка/переустановка библиотек (включая NVIDIA), опция Force Reinstall.
• [Обновления] — проверка новых версий на PyPI и установка обновлений (выборочное обновление; несовместимые версии пропускаются).
• [Очистить лог] — очистка окна лога.
• [Каталог сохранения] — каталог для выходных файлов. Можно ввести полный путь (например D:\Whisper) или имя подкаталога (символы, недопустимые в Windows, заменяются на _). При указании каталога он создаётся при необходимости, и все выходные файлы сохраняются в него. Если поле пустое — файлы сохраняются рядом с исходным.
• [Help] — эта справка.

Чекбоксы: "Воспроизвести звук по завершении очереди", "Сохранить извлечённое аудио (MP3)".

Слежение за каталогом (справа от кнопки [Каталог сохранения], через |):
• Отметка "Слежение за каталогом" и поле ввода пути. Если при установке отметки путь пуст — открывается диалог выбора каталога. Путь можно ввести вручную или вставить из буфера (Ctrl+V). Кнопка выбора каталога не используется.
• При включённой отметке программа следит за указанным каталогом: как только появляется новый файл (поддерживаемый формат) — он сразу обрабатывается с текущими настройками (язык, устройство, сохранение MP3, каталог вывода). После обработки слежение продолжается.

=== ПОВЕДЕНИЕ КЛАВИШ ===
При активном окне приложения:
• Enter — если очередь пуста: открывается диалог «Добавить файлы»; если в очереди есть файлы: запуск транскрибации (как кнопка [Старт]).
• Пробел (Space) — переключает отметку «Сохранить Mp3» (включено/выключено). В полях ввода («Каталог сохранения», «Слежение за каталогом») пробел работает как обычно (ввод символа).
• Ctrl+V — вставка пути из буфера обмена в поле «Слежение за каталогом».

=== ПОЛЕЗНЫЕ ФИШКИ ===
• Drag & Drop: перетащите файлы в окно; перетаскивание внутри списка — изменение порядка очереди.
• Смарт-очередь: обработанные файлы помечаются "– обработан".
• Ссылки в логе: клик — открыть файл; Shift+клик — открыть папку с файлом в проводнике.
• Контекстное меню лога: правый клик — копирование текста.
• Прогресс-бар и временные метки сегментов в процессе обработки.
• Звук по завершении: ищет .mp3 в папке приложения; иначе системный звук Windows (Alarm03.wav).
• Сохранение аудио в MP3: при включённой опции — файл <имя>_audio.mp3.
• Слежение за каталогом: при включённой отметке новые файлы в указанной папке обрабатываются автоматически; путь можно ввести вручную или вставить (Ctrl+V).
• VAD фильтр: Voice Activity Detection включен по умолчанию (улучшает качество).
• Иконка: окно и панель задач используют favicon.ico из папки проекта.

=== УСТАНОВКА И НАСТРОЙКА ===

ПЕРВИЧНАЯ УСТАНОВКА:
1. Установите Python 3.9+ (рекомендуется 3.10+). При установке отметьте "Add Python to PATH".
   https://www.python.org/downloads/

2. Установите FFmpeg и добавьте в PATH.
   https://ffmpeg.org/download.html

3. Запустите install.bat. Скрипт:
   • Проверяет Python и уже установленные пакеты
   • Обновляет pip, setuptools, wheel
   • Устанавливает PyTorch (CUDA 12.1), faster-whisper, ctranslate2
   • Пропускает nvidia-cublas-cu12, nvidia-cudnn-cu12 при первой установке (устанавливаются из GUI: [Обновления] или [Зависимости])
   • Устанавливает pygame, pydub, tkinterdnd2-universal
   • Проверяет пакеты, FFmpeg и CUDA

4. Запуск приложения:
   • run_whisper.vbs — двойной клик (окно консоли не показывается).
   • Ярлык (Whisper Fast GUI.lnk) можно создать вручную: цель — run_whisper.vbs или main.py.
   Для отладки из CMD: py main.py (из папки проекта).

АВТОМАТИЧЕСКАЯ УСТАНОВКА:
При первом запуске скрипт проверяет и устанавливает недостающие зависимости (без NVIDIA-компонентов). NVIDIA-библиотеки — только через [Обновления] или [Зависимости] в GUI.
Опция Force Reinstall — в кнопке [Зависимости].

УСТАНОВКА ВРУЧНУЮ:
Командная строка в папке проекта: python installer.py (или установка пакетов через pip по отдельности).

=== ТЕХНИЧЕСКИЕ ЗАВИСИМОСТИ ===
При первичной установке не ставятся: nvidia-cublas-cu12, nvidia-cudnn-cu12. Их установка — из GUI ([Обновления] или [Зависимости]).

При [Зависимости] / [Обновления] устанавливаются/обновляются:
• Базовые: pip, setuptools, wheel
• AI (CUDA 12.1): torch, torchvision, torchaudio, faster-whisper, ctranslate2, nvidia-cublas-cu12, nvidia-cudnn-cu12
• Мультимедиа: pydub, pygame, tkinterdnd2-universal
• Python 3.13+: pyaudioop (при необходимости устанавливается автоматически)
• Системные: ffmpeg (отдельная установка в PATH)

=== БЕЗОПАСНОСТЬ И СТАБИЛЬНОСТЬ ===
• Безопасное завершение: при закрытии приложения модель выгружается из памяти, очищается кэш CUDA.
• Отмена операции: мгновенная остановка с корректным завершением текущего сегмента.
• Обработка ошибок: логирование и корректная реакция на сбои.
