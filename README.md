# Whisper Fast GUI

Графічний інтерфейс для транскрибації аудіо та відео на основі Faster-Whisper (OpenAI Whisper).

---

## Використання на різних ОС

Скрипт підтримує Windows, Linux та macOS.

### Windows 10/11
- **Запуск:** подвійний клік по `main.py` або через ярлик (`Whisper Fast GUI.lnk`).
- **Без консолі:** `run_whisper.vbs`.
- **Встановлення залежностей:** `install.bat` (або кнопка [Залежності] в програмі).
- **Клік по посиланню в логу:** відкриття файлу; Shift+клік — відкрити папку та виділити файл у провіднику.
- **Звук завершення:** за відсутності .mp3 у папці використовується системний звук Windows.

### Linux
- **Запуск:** з каталогу проекту виконати: `python3 main.py` (або `python main.py`).
- **Drag & Drop і відображення:** потрібен робочий стіл з підтримкою X11/Wayland та tkinter.
- **Встановлення:** встановіть Python 3, FFmpeg та залежності (через pip або кнопку [Залежності]).
- **Відкриття файлів і папок з логу:** використовується `xdg-open` (пакет `xdg-utils`).
- **Звук завершення:** за відсутності .mp3 — спроба відтворити системний звук (freedesktop); інакше звук не відтворюється.

### macOS
- **Запуск:** `python3 main.py` з каталогу проекту.
- **Відкриття файлів з логу:** `open` (звичайний клік) та `open -R` (Shift+клік — показати у Finder).
- **Звук завершення:** як на Linux (системні звуки за відсутності .mp3).

На всіх ОС потрібні: **Python 3.9+**, **FFmpeg** у PATH, встановлені залежності (faster-whisper, torch, pydub тощо). Рекомендовано відеокарту NVIDIA з CUDA для прискорення на Windows та Linux.

---

## Про обладнання та скрипт

Скрипт базується на технології OpenAI Whisper та бібліотеці Faster-Whisper. Використовується модель **large-v3-turbo** для високої точності та швидкості. Версія та дата застосунку відображаються у верхній частині вікна (справа від кнопок).

---

## Структура проекту

```
WhisperFastGUI/
├── main.py              — точка входу: перевірка залежностей, іконка панелі задач, запуск GUI
├── gui.py               — головне вікно: черга, налаштування, транскрибація, лог
├── model_manager.py     — завантаження та вивантаження моделі Whisper (Singleton)
├── input_files.py       — додавання файлів/каталогів, валідація, Drag & Drop
├── lang_manager.py      — переклади інтерфейсу (EN/UK/RU), завантаження lang.json
├── i18n_fallback.py     — резервні функції t/set_language при недоступності lang_manager
├── config.py            — константи: версія, формати, модель, мови
├── utils.py             — форматування часу, звук завершення
├── installer.py         — встановлення та оновлення залежностей, перевірка системи
├── lang.json            — тексти інтерфейсу трьома мовами
├── settings.json        — збережені налаштування (створюється при першому збереженні)
├── README.md            — ця довідка
├── favicon.ico          — іконка вікна та панелі задач
├── install.bat          — первинне встановлення залежностей
├── run_whisper.vbs      — запуск без вікна консолі
└── __pycache__/         — кеш Python (створюється автоматично)
```

Опційно в папці: файл .mp3 для звуку завершення.

---

## Технічні вимоги

- **Python:** 3.9 або новіший (рекомендовано 3.10+). Підтримується Python 3.13+.
- **ОС:** Windows 10/11, Linux (X11/Wayland), macOS.
- **GPU:** Рекомендована відеокарта NVIDIA з підтримкою CUDA 12.1. AMD Radeon та інші GPU без CUDA працюють лише в режимі CPU.
- **FFmpeg:** Потрібен у PATH для обробки медіафайлів.
- **Python 3.13+:** Автоматично встановлюється pyaudioop (заміна прибраного модуля audioop).

---

## Підтримувані формати

| Тип   | Формати                          |
|-------|----------------------------------|
| Аудіо | .mp3, .wav, .m4a, .flac, .ogg   |
| Відео | .mp4, .mkv, .avi, .mov          |

Потрібен FFmpeg у PATH для декодування медіафайлів.

---

## Функціонал

Перетворює мовлення з аудіо та відео на текст. Вихідні файли створюються у папці збереження або поруч із вихідним файлом:

1. **.txt** — чистий текст транскрибації (кожен сегмент з нового рядка).
2. **.srt** — субтитри: нумерація сегментів, часові мітки, текст.

Опційно (при увімкненій опції «Зберегти витягнуте аудіо (MP3)»):
3. **_audio.mp3** — витягнута аудіодоріжка.

---

## Режими обробки

- **Один файл у черзі:** виберіть файл і натисніть [Старт] — діалог не показується.
- **Кілька файлів:** при натисканні [Старт] з’являється діалог: «Тільки обраний файл», «Усі файли в черзі» або «Скасувати».
- **Тільки нові:** обробляються лише файли без позначки «– оброблено».
- **Вся черга:** обробка всіх файлів по порядку.

---

## Налаштування пристрою та виводу

- **AUTO** — автоматичний вибір (відеокарта NVIDIA за наявності, інакше CPU).
- **GPU** — примусово відеокарта (якщо CUDA доступна). Для старих карт — безпечний режим int8.
- **CPU** — примусово процесор, int8 (підходить також для AMD Radeon та інших GPU без CUDA).

Модель завантажується один раз і використовується для всіх файлів (Singleton).

**Додаткові опції:**
- Відтворити звук по завершенні черги.
- Зберегти витягнуте аудіо (MP3) — для кожного файлу зберігається `<ім'я>_audio.mp3`.

---

## Мови інтерфейсу

- **EN** — англійська (за замовчуванням), **UK** — українська, **RU** — російська.
- Перемикач (🌐 EN UK RU) у верхній частині вікна. Мова зберігається в `settings.json`.
- Мова інтерфейсу не впливає на розпізнавання мовлення. Мову розпізнавання задає перемикач «Мова розпізнавання» (AUTO, RU, UK, EN).

---

## Опис кнопок

| Кнопка               | Дія |
|----------------------|-----|
| [Старт]              | Запуск обробки (з вибором режиму) |
| [Скасувати]          | Зупинка поточної задачі |
| [Додати файли]       | Вибір одного або кількох файлів |
| [Додати каталог]     | Додавання всіх підтримуваних файлів з каталогу рекурсивно |
| [Очистити чергу]     | Видалення всіх файлів зі списку |
| [Система]            | Перевірка GPU та FFmpeg |
| [Залежності]         | Встановлення або перевстановлення бібліотек, опція Force Reinstall |
| [Оновлення]          | Перевірка нових версій на PyPI та встановлення оновлень |
| [Очистити лог]       | Очищення вікна логу |
| [Каталог збереження] | Каталог для вихідних файлів (порожнє поле — збереження поруч із вихідним) |
| [Довідка]            | Ця довідка |

**Прапорці:** «Відтворити звук по завершенні черги», «Зберегти витягнуте аудіо (MP3)».

**Слідкування за каталогом:** при увімкненій позначці програма слідкує за вказаним каталогом — нові файли обробляються автоматично з поточними налаштуваннями.

---

## Поведінка клавіш

- **Enter** — якщо черга порожня: відкривається діалог «Додати файли»; якщо є файли: запуск транскрибації (як кнопка [Старт]).
- **Пробіл (Space)** — перемикає позначку «Зберегти Mp3» (у полях вводу працює як звичайний пробіл).
- **Ctrl+V** — вставка шляху з буфера обміну в поле «Слідкування за каталогом».

---

## Корисні можливості

- **Drag & Drop:** перетягування файлів у вікно; перетягування всередині списку змінює порядок черги.
- **Розумна черга:** оброблені файли позначаються «– оброблено».
- **Посилання в логу:** клік — відкрити файл; Shift+клік — відкрити папку з файлом у провіднику.
- **Контекстне меню логу:** правий клік — копіювання тексту.
- **Прогрес-бар** та часові мітки сегментів під час обробки.
- **Фільтр VAD:** Voice Activity Detection увімкнено за замовчуванням.
- **Іконка:** вікно та панель задач використовують `favicon.ico` з папки проекту.

---

## Встановлення та налаштування

### Первинне встановлення

1. **Встановіть Python 3.9+** (рекомендовано 3.10+). При встановленні позначте «Add Python to PATH».  
   https://www.python.org/downloads/

2. **Встановіть FFmpeg** і додайте його в PATH.  
   https://ffmpeg.org/download.html

3. **Запустіть `install.bat`.** Скрипт:
   - перевіряє Python та вже встановлені пакети;
   - оновлює pip, setuptools, wheel;
   - встановлює PyTorch (CUDA 12.1), faster-whisper, ctranslate2;
   - пропускає nvidia-cublas-cu12 та nvidia-cudnn-cu12 при першому встановленні (їх можна встановити з GUI: [Оновлення] або [Залежності]);
   - встановлює pygame, pydub, tkinterdnd2-universal;
   - перевіряє пакети, FFmpeg та CUDA.

4. **Запуск:**
   - `run_whisper.vbs` — подвійний клік (вікно консолі не показується).
   - Ярлик можна створити вручну: ціль — `run_whisper.vbs` або `main.py`.  
   Для відлагодження з CMD: `py main.py` (з папки проекту).

### Автоматичне встановлення

При першому запуску скрипт перевіряє та встановлює відсутні залежності (без компонентів NVIDIA). Бібліотеки NVIDIA встановлюються лише через [Оновлення] або [Залежності] в GUI.

### Встановлення вручну

У командному рядку в папці проекту виконайте: `python installer.py`.

---

## Технічні залежності

При первинному встановленні не встановлюються: `nvidia-cublas-cu12`, `nvidia-cudnn-cu12`. Їхнє встановлення — з GUI ([Оновлення] або [Залежності]).

При [Залежності] / [Оновлення] встановлюються або оновлюються:
- **Базові:** pip, setuptools, wheel
- **AI (CUDA 12.1):** torch, torchvision, torchaudio, faster-whisper, ctranslate2, nvidia-cublas-cu12, nvidia-cudnn-cu12
- **Мультимедіа:** pydub, pygame, tkinterdnd2-universal
- **Python 3.13+:** pyaudioop (за потреби встановлюється автоматично)
- **Системні:** ffmpeg (окреме встановлення в PATH)

---

## Безпека та стабільність

- **Безпечне завершення:** при закритті застосунку модель вивантажується з пам’яті, очищується кеш CUDA.
- **Скасування операції:** миттєва зупинка з коректним завершенням поточного сегмента.
- **Обробка помилок:** логування та коректна реакція на збої.
