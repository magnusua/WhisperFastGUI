# Whisper Fast GUI

**Версія:** 1.2.1
**Дата публікації:** 22.07.2026

Графічний інтерфейс для транскрибації аудіо та відео на основі Faster-Whisper (OpenAI Whisper). Також обробляє текстові/офісні документи (конвертація в Markdown, опційно Cursor і Word).

Репозиторій: https://github.com/magnusua/WhisperFastGUI

---

## Використання на різних ОС

Скрипт підтримує Windows, Linux та macOS.

### Windows 10/11
- **Запуск:** подвійний клік по `main.py` або через ярлик (`Whisper Fast GUI.lnk`).
- **Запуск з автоматичним стартом черги:** у консолі з каталогу проекту: `python main.py --transcribe` — програма відкриється і через пів секунди автоматично почне обробку всієї поточної черги (якщо черга не порожня).
- **Без консолі:** `run_whisper.vbs`.
- **Встановлення залежностей:** `install.bat` (або кнопка [Залежності] в програмі).
- **Модель:** кнопка з назвою поточної моделі (наприклад «large-v3-turbo») — ліворуч від перемикача «Трей» — відкриває діалог вибору моделі Whisper; у діалозі є кнопки **«Завантажити модель»** та **«Оновити модель»** (перевірка нової ревізії на Hugging Face Hub).
- **Панель / Трей / Панель + Трей:** перемикач у нижній панелі. У режимі **Трей** закриття вікна (×) не закриває програму — вона ховається в системний трей і працює далі; вихід — через меню іконки в треї. Детально — розділ «Режими Панель / Трей / Панель + Трей» нижче.
- **Автозапуск:** кнопка [Автозапуск] у програмі (поруч із перемикачем «Трей») запускає `autorun_delayed.bat` і додає програму в автозавантаження з затримкою 25 с.
- **Клік по посиланню в логу:** відкриття файлу; Shift+клік — відкрити папку та виділити файл у провіднику.
- **Звук завершення:** за відсутності файлу .mp3 у **resources/** (або в каталозі програми) використовується системний звук Windows.

### Linux
- **Запуск:** з каталогу проекту виконати: `python3 main.py` (або `python main.py`). Для автоматичного старту обробки черги: `python3 main.py --transcribe`.
- **Drag & Drop і відображення:** потрібен робочий стіл з підтримкою X11/Wayland та tkinter.
- **Встановлення:** встановіть Python 3, FFmpeg та залежності (через pip або кнопку [Залежності]).
- **Відкриття файлів і папок з логу:** використовується `xdg-open` (пакет `xdg-utils`).
- **Звук завершення:** за відсутності .mp3 у **resources/** — спроба відтворити системний звук (freedesktop); інакше звук не відтворюється.

### macOS
- **Запуск:** `python3 main.py` з каталогу проекту. Для автоматичного старту обробки черги: `python3 main.py --transcribe`.
- **Відкриття файлів з логу:** `open` (звичайний клік) та `open -R` (Shift+клік — показати у Finder).
- **Звук завершення:** як на Linux (системні звуки за відсутності .mp3 у resources/).

На всіх ОС потрібні: **Python 3.9–3.13** (на практиці найстабільніше **3.11 або 3.12**), **FFmpeg** у PATH, залежності (faster-whisper, torch, pydub тощо). **Python 3.14+** часто не підходить: для нього ще немає коліс **ctranslate2** / **torch** на PyPI — встановлення падає; використовуйте 3.12. Рекомендовано відеокарту NVIDIA з CUDA для прискорення на Windows та Linux.

---

## Про обладнання та скрипт

Скрипт базується на технології OpenAI Whisper та бібліотеці Faster-Whisper. За замовчуванням використовується модель **large-v3-turbo** (оптимальний баланс якості та швидкості). Модель можна змінити: кнопка з назвою поточної моделі (ліворуч від перемикача «Трей») відкриває діалог вибору (tiny, base, small, medium, large-v1/v2/v3, large-v3-turbo, distil-large-v3). У діалозі відображається, які моделі вже завантажені та їхній приблизний розмір; кнопка **«Завантажити модель»** дозволяє завантажити обрану модель у пам’ять одразу, без очікування запуску транскрибації; кнопка **«Оновити модель»** — завантажити нову ревізію ваг з Hugging Face Hub. Версія та дата застосунку відображаються у верхній частині вікна (справа від кнопок).

---

## Структура проекту

```
WhisperFastGUI/
├── main.py                 — точка входу: залежності, іконка панелі задач, запуск GUI
├── whisperfast/            — пакет застосунку
│   ├── config.py           — BASE_DIR, RESOURCES_DIR, константи, версія з README, довідка
│   ├── settings.py         — читання/запис settings.json
│   ├── platform_util.py    — win_no_window_kwargs (без вікон консолі на Windows)
│   ├── utils.py            — час, шляхи черги, тривалість аудіо, звук завершення
│   ├── core/
│   │   ├── model_manager.py    — завантаження/вивантаження Whisper (Singleton)
│   │   ├── transcription.py    — пайплайн черги: медіа (TXT/SRT/MP3) і документи (MD)
│   │   ├── document_convert.py — PDF/DOC/DOCX/текст → Markdown (markitdown)
│   │   ├── pandoc_export.py    — Markdown → DOCX через Pandoc (опційно)
│   │   ├── queue_manager.py    — request_queue.json, слідкування за каталогами
│   │   └── input_files.py      — додавання файлів/каталогів, валідація, DnD
│   ├── ui/
│   │   ├── gui.py          — головне вікно (оркестрація)
│   │   ├── widgets.py      — Tooltip, константи масштабу UI
│   │   ├── tray.py         — системний трей
│   │   └── dialogs.py      — діалоги моделі, виводу, Cursor, довідка
│   ├── i18n/
│   │   ├── __init__.py     — t / set_language / get_language
│   │   ├── lang_manager.py — переклади EN/UK/RU
│   │   ├── fallback.py     — резерв при недоступності перекладів
│   │   └── lang.json       — тексти інтерфейсу
│   ├── setup/
│   │   ├── installer.py        — pip-залежності, перевірка системи
│   │   ├── python_selector.py  — вибір Python → settings.json
│   │   └── gpu_info.py         — NVIDIA GPU → settings.json
│   ├── updates/
│   │   ├── app_updates.py      — оновлення програми з GitHub
│   │   └── model_updates.py    — оновлення ваг Whisper (HF Hub)
│   └── postprocess/
│       └── cursor_postprocess.py — постпроцесинг TXT/MD через Cursor
├── resources/
│   ├── favicon.ico         — іконка вікна / панелі задач / трею
│   ├── m.mp3               — звук завершення (опційно; шукається в resources/)
│   ├── Help_EN.md / Help_UK.md / Help_RU.md — коротка довідка
│   └── templates/          — опційно: reference.docx для стилів Pandoc
├── redactor1.md            — нумеровані промпти для Cursor (редагується з GUI)
├── settings.json           — збережені налаштування користувача
├── request_queue.json      — збережена черга файлів
├── README.md               — опис програми; єдине джерело версії та дати
├── install.bat             — первинне встановлення залежностей
├── run_whisper.vbs         — запуск без вікна консолі
├── start_delayed.vbs       — запуск з затримкою (автозавантаження)
└── autorun_delayed.bat     — додати програму в автозавантаження
```

Усі шляхи runtime (`settings.json`, `request_queue.json`, `redactor1.md`, ярлики) базуються на каталозі з `main.py`. Ассети (іконка, Help, звук) — у `resources/`.

---

## Автозапуск з затримкою (Windows)

Щоб програма запускалася при вході в систему з затримкою 20–30 секунд:

**Спосіб 1 (рекомендовано):** у програмі натисніть кнопку **[Автозапуск]** (у нижній панелі після перемикача «Трей»). Відкриється вікно скрипта; після виконання ярлик з’явиться в каталозі Автозавантаження.

**Спосіб 2:** запустіть **`autorun_delayed.bat`** з папки проекту (подвійний клік або з CMD). Скрипт створить ярлик у каталозі Автозавантаження; після наступного входу в Windows програма запуститься автоматично через 25 секунд. Скрипт написаний одним рядком виклику PowerShell (без продовження рядків), щоб коректно працювати на різних ПК і при різних закінченнях рядків у файлі.

**Спосіб 3 (вручну):**
1. Відкрийте папку **Автозавантаження**: натисніть `Win+R`, введіть `shell:startup`, Enter.
2. Створіть **ярлик** на файл `start_delayed.vbs` з папки проекту і перемістіть його в папку Автозавантаження.
3. За замовчуванням затримка — **25 секунд**. Щоб змінити, відредагуйте `start_delayed.vbs`: змініть значення `delaySec = 25` на 20 або 30.

Права адміністратора не потрібні: ярлик створюється в каталозі автозавантаження поточного користувача (`%APPDATA%\...\Startup`).

Після входу в Windows програма запуститься автоматично через вказану кількість секунд (без вікна CMD).

---

## Режими «Панель» / «Трей» / «Панель + Трей» та робота з системним треєм

У нижній панелі програми є перемикач режиму відображення: **Панель**, **Трей**, **Панель + Трей**. Від нього залежить, де з’являється програма (панель задач, область сповіщень біля годинника, чи обидва) і **що відбувається при натисканні кнопки закриття вікна (×)**.

Потрібні бібліотеки **pystray** та **Pillow** (кнопки [Залежності] або [Оновлення]). Якщо вони не встановлені, режими «Трей» та «Панель + Трей» недоступні (у лозі з’явиться попередження).

### Режим «Панель»
- Відображається лише на **панелі задач**; іконки в системному треї немає.
- **Закриття вікна (×):** з’являється діалог «Закрити програму?» — підтвердження закриває застосунок.

### Режим «Трей»
- Програма показується лише як **іконка в системному треї** (область сповіщень біля годинника). Вікно при запуску приховане; подвійний клік по іконці або пункт меню **«Показати вікно»** відкривають вікно.
- **Закриття вікна (×):** вікно **не закривається** — воно ховається в трей, програма продовжує працювати у фоні (зокрема може працювати черга та слідкування за каталогом).
- **Повне закриття програми:** у контекстному меню іконки в треї виберіть **«Вихід»** — з’явиться звичайний діалог підтвердження закриття.

### Режим «Панель + Трей»
- Програма одночасно на **панелі задач** і в **системному треї**.
- **Закриття вікна (×):** як у режимі «Панель» — з’являється діалог закриття; підтвердження закриває застосунок.
- Якщо потрібно лишити програму працювати у фоні без закриття — згорніть вікно кнопкою «−» (мінімізація), а не закривайте через ×.

### Підсумок
| Режим           | × закриває вікно? | Де закрити програму повністю?        |
|-----------------|-------------------|--------------------------------------|
| Панель          | Так (через діалог)| Кнопка × → «Так» у діалозі           |
| Трей            | Ні (ховає в трей) | Меню іконки в треї → «Вихід» → «Так» |
| Панель + Трей   | Так (через діалог)| Кнопка × → «Так» у діалозі           |

---

## Технічні вимоги

- **Python:** 3.9–3.13; рекомендовано **3.11 або 3.12**. Python 3.14+ може не мати сумісних збірок torch/ctranslate2.
- **Вибір Python:** під час першого запуску програма знаходить установлені версії. Якщо їх кілька, користувач обирає версію за замовчуванням; шлях і версія зберігаються в `settings.json`.
- **ОС:** Windows 10/11, Linux (X11/Wayland), macOS.
- **GPU:** Рекомендована відеокарта NVIDIA з підтримкою CUDA 12.1. AMD Radeon та інші GPU без CUDA працюють лише в режимі CPU.
- **FFmpeg:** Потрібен у PATH для обробки медіафайлів.
- **Python 3.13+:** Автоматично встановлюється pyaudioop (заміна прибраного модуля audioop).

---

## Підтримувані формати

| Тип        | Формати |
|------------|---------|
| Аудіо      | .mp3, .wav, .m4a, .flac, .ogg |
| Відео      | .mp4, .mkv, .avi, .mov |
| Текст      | .md, .markdown, .txt, .text, .rst, .csv, .html, .htm |
| Документи  | .pdf, .doc, .docx (конвертуються в Markdown) |

Для аудіо/відео потрібен FFmpeg у PATH. Для PDF/DOC/DOCX — пакет `markitdown` (ставиться через [Залежності] / [Оновлення]).

---

## Черга (таблиця)

Підпис блоку — **«Черга»** (або «Queue» / «Очередь» залежно від мови). При наведенні курсора на цей підпис з’являється підказка з поясненням роботи таблиці та відрізків часу.

Черга відображається як **таблиця** з колонками:

| Колонка           | Опис |
|------------------|------|
| **#**            | Порядковий номер. |
| **Назва файлу**  | Ім'я файлу (плюс «– оброблено», якщо вже оброблений). |
| **Початок**      | Час початку обробки (за замовчуванням 00:00:00,000). |
| **Кінець відр. 1** | Кінець першого відрізка (за замовчуванням порожньо). |
| **Кінець відр. 2** | Кінець другого відрізка (за замовчуванням порожньо). |
| **Кінець**       | Час кінця обробки (за замовчуванням — тривалість файлу). |

- При додаванні файлу **Початок** = 00:00:00,000, **Кінець** = тривалість файлу (з ffprobe/pydub).
- **Подвійний клік** по рядку — діалог редагування діапазону часу (Початок, Кінець відр. 1/2, Кінець).
- **Shift + клік** по рядку — відкрити розташування вихідного файла у файловому менеджері (на Windows: папка відкривається з виділенням файла).
- **Delete** або **ПКМ → «Видалити»** — прибрати виділений файл (або групу виділених через Ctrl/Shift) з черги.
- Черга **зберігається** в `request_queue.json` (додавання, очищення, перетягування, редагування, видалення).
- При **запуску** програми черга підвантажується з `request_queue.json` (файли, яких уже немає на диску, пропускаються).

Обробка виконується лише в діапазоні **[Початок — Кінець]** для кожного рядка. Так можна обробити один файл частинами (наприклад 0–20 хв англійською, 20–60 хв російською).

---

## Функціонал

Перетворює мовлення з аудіо та відео на текст. Вихідні файли створюються у каталозі збереження або поруч із вихідним файлом:

1. **.txt** — чистий текст транскрибації (кожен сегмент з нового рядка).
2. **.srt** — субтитри: нумерація сегментів, часові мітки, текст.

Опційно (при увімкненій опції «Зберегти витягнуте аудіо (MP3)»):
3. **_audio.mp3** — витягнута аудіодоріжка (для повного файлу — один файл; для відрізка — окремий файл з суфіксом часу).

**Обробка по діапазону (відрізками):** якщо в таблиці задано Початок і/або Кінець відмінні від 0 і тривалості файлу, обробляється лише цей відрізок. Результати зберігаються в **окремі файли** з суфіксом часу, наприклад:
- `ім'я_00-00-00_00-20-00.txt` / `.srt` — відрізок 0:00–0:20;
- `ім'я_00-20-00_01-00-00.txt` / `.srt` — відрізок 0:20–1:00.
При увімкненому «Зберегти Mp3» для кожного відрізка створюється окремий `ім'я_..._audio.mp3` за тим самим діапазоном.

### Документи та текст у черзі

У чергу можна додавати текстові й офісні файли (не лише аудіо/відео). Whisper для них **не** викликається.

1. **.pdf / .doc / .docx** — конвертація в **.md** (через `markitdown`); у лог пишеться, що **вихідний** файл у Cursor не передавався.
2. **.md** та інші текстові формати — готується Markdown у каталозі збереження (копія/запис `.md`).
3. Якщо увімкнено **«У Cursor»** — у Cursor передається **Markdown** для стандартного прогону промптів із `redactor1.md`; кроки й результати видно в логу.
4. Якщо увімкнено **«MD → Word»** — після готовності MD (або після кожного результату Cursor) створюється **.docx** через **Pandoc** (має бути в PATH). Опційні стилі Word: `resources/templates/reference.docx`.

---

## Режими обробки

- **Один файл у черзі:** виберіть файл і натисніть [Старт] — діалог не показується.
- **Кілька файлів:** при натисканні [Старт] з’являється діалог: «Тільки обраний файл», «Усі файли в черзі» або «Скасувати».
- **Тільки нові:** обробляються лише файли без позначки «– оброблено».
- **Вся черга:** обробка всіх файлів по порядку.

---

## Налаштування пристрою та моделі

- **AUTO** — автоматичний вибір (відеокарта NVIDIA за наявності, інакше CPU).
- **GPU** — примусово відеокарта (якщо CUDA доступна). Для старих карт — безпечний режим int8.
- **CPU** — примусово процесор, int8 (підходить також для AMD Radeon та інших GPU без CUDA).

**Вибір моделі:** кнопка з назвою поточної моделі (наприклад «large-v3-turbo») відкриває діалог, де можна обрати модель зі списку (tiny, base, small, medium, large-v1/v2/v3, large-v3-turbo, distil-large-v3), переглянути, які з них уже завантажені та їхній розмір, натиснути **«Завантажити модель»**, щоб одразу завантажити обрану модель у пам’ять (без очікування старту транскрибації), або **«Оновити модель»** — щоб завантажити нову ревізію ваг з Hugging Face Hub (у списку з’являється позначка «є оновлення», якщо на Hub є новіша версія). Модель завантажується один раз і використовується для всіх файлів (Singleton). Завантажені моделі зберігаються в кеші Hugging Face Hub (шлях показано в підказці до кнопки та в діалозі).

**Додаткові опції:**
- Відтворити звук по завершенні черги.
- Зберегти витягнуте аудіо (MP3) — для повного файлу один `<ім'я>_audio.mp3`; для відрізка — окремий файл з суфіксом часу (наприклад `<ім'я>_00-20-00_01-00-00_audio.mp3`).
- **У Cursor** — після створення `.txt` (транскрибація) або `.md` (документи), із затримкою 5 с, обробка промптами з `redactor1.md`.
- **MD → Word** — експорт Markdown у `.docx` через Pandoc (після MD або після кожного результату Cursor).

---

## Постпроцесинг TXT/MD у Cursor

1. Увімкніть прапорець **«У Cursor»**.
2. За потреби змініть промпти кнопкою підпису **«У Cursor»** / відкриттям `redactor1.md` (секції `## Промпт №1`, `## Промпт №2`, …).
3. API-ключ (опційно): натисніть **[api_key]**, введіть ключ у окремому вікні та натисніть **«Зберегти»**. Закриття через X не змінює ключ. Значення зберігається у `settings.json`; змінна середовища `CURSOR_API_KEY` має пріоритет.

Вхід для Cursor: `.txt` після транскрибації або `.md` після обробки документа. Для PDF/DOC/DOCX у лог записується, що **вихідний** файл у Cursor не передавався (передається лише Markdown).

**З ключем (Cursor SDK):** автоматичний ланцюжок. Пакет `cursor-sdk` ставиться через [Залежності] / [Оновлення] (або `pip install cursor-sdk`).
Якщо пакет відсутній — з’явиться запит на встановлення; без нього використовується режим Chat.
- Промпт №1 `"redactor"` → `<ім'я>_redactor.md`
- Промпт №N `"TW_core"` → `<ім'я>_TW_core.md` (вхід — попередній результат)
- Назва береться з лапок у заголовку (`## Промпт №2 "TW_core"`). Без лапок — запасний варіант `*_edited.md` / `*_edited_N.md`.
- Порожні секції промптів пропускаються без зупинки ланцюжка. Наприклад, якщо №2 порожній, виконуються №1, №3, №4…; файл для №2 не створюється, а №3 отримує результат останнього виконаного промпту.
У лог пишеться кожен створений файл зі посиланням.

**Без ключа:** відкривається **Cursor Chat** з TXT; промпт №1 копіюється в буфер — потрібне ручне підтвердження. Автоланцюжок 2..N у цьому режимі не виконується.

---

## Мови інтерфейсу

- **EN** — англійська (за замовчуванням), **UK** — українська, **RU** — російська.
- Перемикач (🌐 EN UK RU) у верхній частині вікна. Мова зберігається в `settings.json`.
- Кнопка **[Довідка]** відкриває текст довідки тією ж мовою, що й інтерфейс (`resources/Help_EN.md` / `Help_UK.md` / `Help_RU.md`).
- Мова інтерфейсу не впливає на розпізнавання мовлення. Мову розпізнавання задає перемикач «Мова розпізнавання» (AUTO, RU, UK, EN).

---

## Опис кнопок

| Кнопка               | Дія |
|----------------------|-----|
| [Старт]              | Запуск обробки (з вибором режиму) |
| [Скасувати]          | Зупинка поточної задачі |
| [Додати файли]       | Вибір одного або кількох файлів |
| [Додати каталог]     | Додавання всіх підтримуваних файлів з каталогу рекурсивно |
| [Очистити чергу]     | Видалення всіх файлів зі списку |
| [Система]            | Перевірка GPU (NVIDIA), PyTorch/CUDA, FFmpeg та Pandoc; модель відеокарти зберігається в `settings.json` |
| [Залежності]         | Встановлення або перевстановлення бібліотек (у т. ч. pystray, Pillow, cursor-sdk, markitdown), опція Force Reinstall; помилки pip виводяться в лог |
| [Оновлення]          | Перевірка оновлень: спочатку версія програми на GitHub, далі pip-пакети (для NVIDIA — torch з індексу CUDA 12.1 / cu121), моделі Whisper на Hugging Face Hub; після підтвердження встановлює вибране |
| Кнопка моделі        | Діалог вибору моделі Whisper: список (завантажені / не завантажені), «Завантажити модель», «Оновити модель», «OK», «Скасувати» |
| [Очистити лог]       | Очищення вікна логу |
| [Автозапуск]         | Запуск `autorun_delayed.bat`: додає програму в автозавантаження Windows з затримкою 25 с (тільки Windows) |
| [Сохранение]         | Налаштування збереження всіх файлів програми (поруч із відео / каталог / підкаталог `{basename}`) |
| [mp3]                | Чекбокс увімкнення MP3; кнопка — місце збереження MP3 (як у Сохранение / поруч із відео / окремий каталог) |
| [Довідка]            | Ця довідка |
| [В Cursor]           | Чекбокс постпроцесингу; кнопка відкриває файл промптів Cursor у системному редакторі |
| [api_key]            | Відкрити окреме вікно для введення та збереження Cursor API-ключа |
| [MD → Word]          | Чекбокс експорту Markdown у Word (.docx) через Pandoc |

**Прапорці:** «Відтворити звук по завершенні черги»; **mp3**, **Слідкування**, **В Cursor**, **MD → Word** — чекбокс окремо (для Cursor підпис-кнопка відкриває промпти).

**Слідкування:** чекбокс + кнопка **Слідкування** (список каталогів у `settings.json` через кому). Опитування кожні 10 с; новий файл → pending (вік ≥ 10 с, розмір стабільний ~15 с, файл відкривається) → черга → автообробка. При помилці декодування — до 2 retry. Власні `.txt`/`.srt`/`*_audio.mp3` ігноруються.

---

## Поведінка клавіш

- **Enter** — якщо черга порожня: відкривається діалог «Додати файли»; якщо є файли: запуск транскрибації (як кнопка [Старт]).
- **Пробіл (Space)** — перемикає позначку «mp3» (у полях вводу працює як звичайний пробіл).
- **Delete** — видалити виділені файли з черги (коли фокус на таблиці черги).
- **Ctrl+V** — вставка шляху з буфера обміну в поле каталогу збереження або в рядок вікна каталогів слідкування.

---

## Корисні можливості

- **Таблиця черги:** підпис «Черга»; при наведенні на нього — підказка з поясненням колонок та відрізків часу. Шість колонок (№, назва, початок, кінець відр. 1/2, кінець); подвійний клік — редагування часу.
- **Drag & Drop:** перетягування файлів у вікно; перетягування рядків у таблиці змінює порядок черги.
- **Збереження черги:** `request_queue.json` — черга зберігається при змінах і підвантажується при запуску.
- **Розумна черга:** оброблені файли позначаються «– оброблено».
- **Посилання в логу:** клік — відкрити файл; Shift+клік — відкрити папку з файлом у провіднику.
- **Контекстне меню логу:** правий клік — копіювання тексту.
- **Розмір логу:** автоматично обмежується (старі рядки видаляються зверху), щоб уникнути надмірного споживання пам’яті при довгих сесіях.
- **Відлагодження:** якщо перед запуском встановити змінну середовища `DEBUG=1`, у лог виводиться повний traceback при помилках транскрибації.
- **Прогрес-бар** та часові мітки сегментів під час обробки.
- **Фільтр VAD:** Voice Activity Detection увімкнено за замовчуванням.
- **Іконка:** вікно та панель задач використовують `resources/favicon.ico`; для іконки в треї при відсутності файлу використовується резервна сіра іконка.
- **Діалоги:** усі спливаючі вікна (вибір моделі, редагування часу рядка, вибір «тільки обраний / усі файли», довідка) відображаються по центру головного вікна програми (або екрана).
- **Оновлення програми:** кнопка [Оновлення] порівнює локальну версію з метаданими у `README.md` на GitHub (гілка `main`); при наявності git — `git pull`, інакше завантаження ZIP і перезапуск (Windows).
- **Оновлення torch на NVIDIA:** якщо виявлено відеокарту NVIDIA, для torch використовується індекс **CUDA 12.1 (cu121)**, а не звичайний PyPI (щоб не пропонувати CPU-збірку).

---

## Що нового в 1.2.1

- **Документи в черзі:** `.md`, `.txt` та інші текстові формати; `.pdf`, `.doc`, `.docx` конвертуються в Markdown (`markitdown`), без виклику Whisper.
- **Cursor для документів:** у промпти передається Markdown; у логу фіксується, що вихідний PDF/DOC/DOCX у Cursor не передавався; кроки й результати обробки видно в логу.
- **MD → Word:** опція експорту Markdown у `.docx` через Pandoc (після MD або після кожного результату Cursor); перевірка Pandoc у [Система].
- **Оновлення:** перевірка версії програми на GitHub виконується першою в списку [Оновлення].

---

## Що нового в 1.2.0

- **Пакет `whisperfast/`:** код розкладено за шарами (`core`, `ui`, `i18n`, `setup`, `updates`, `postprocess`); `main.py` лишається точкою входу в корені.
- **Ресурси:** іконка, звук і довідка перенесені в `resources/`; `lang.json` — у `whisperfast/i18n/`.
- **Рефакторинг:** транскрибація, діалоги, трей і налаштування винесені з `gui.py`; DRY для subprocess/Windows і парсингу версії з README.

---

## Що нового в 1.1.6

- **Збереження:** кнопка «Сохранение» замість поля шляху; режими — поруч із відео, вибраний каталог або підкаталог із шаблоном `{basename}`.
- **MP3:** чекбокс + кнопка «mp3»; місце збереження — як у «Сохранение», поруч із відео або окремий каталог.
- **Компактний UI:** «Слежение» відкриває список каталогів (без окремої «Каталог»); «В Cursor» відкриває `redactor1.md` (без окремої «Изменить промт»).

---

## Що нового в 1.1.5

- **Слідкування за кількома каталогами:** прапорець «Слідкування» + кнопка «Каталог»; шляхи зберігаються в `settings.json` через кому.
- **Надійне підхоплення завантажень:** новий файл спочатку в pending (вік ≥ 10 с, розмір стабільний ~15 с, файл відкривається); опитування кожні 10 с; при помилці декодування — до 2 retry.
- **Черга винесена в `queue_manager.py`:** persist `request_queue.json`, додавання/видалення та логіка слідкування в одному модулі.
- **Імена файлів Cursor:** замість `*_edited.md` використовується назва промпта з лапок (`## Промпт №2 "TW_core"` → `*_TW_core.md`).

---

## Що нового в 1.1.4

- **Вибір Python при першому запуску:** програма знаходить установлені інтерпретатори, пропонує вибір, якщо їх кілька, та зберігає `python_path` і `python_version` у `settings.json`.
- **Єдина версія Python для запуску й залежностей:** `run_whisper.vbs`, `start_delayed.vbs` та `install.bat` використовують збережений інтерпретатор.
- **Cursor SDK на Windows / Python 3.10–3.11:** додано сумісність для запуску локального bridge та послідовної обробки всіх промптів.
- **Компактний UI:** елементи MP3, каталогів і Cursor згруповано в один рядок; поле каталогу збереження скорочено до третини початкової ширини.
- **API-ключ:** кнопка `api_key` відкриває окреме вікно; ключ змінюється лише після натискання «Зберегти», закриття через X скасовує зміни.
- **Залежності:** `cursor-sdk` перевіряється та встановлюється через [Оновлення] / [Залежності].
- **Черга:** у запиті про обробку тільки нових файлів показується їх кількість.
- **Сигнал завершення:** при ввімкненому «У Cursor» звук відтворюється лише після завершення транскрибації та всієї Cursor-постобробки.

---

## Що нового в 1.1.3

- **Видалення з черги:** виділені файли можна прибрати клавішею Delete або через контекстне меню (ПКМ → «Видалити»); підтримується групове виділення (Ctrl/Shift).
- **Довідка мовою інтерфейсу:** кнопка [Довідка] відкриває `Help_EN.md` / `Help_UK.md` / `Help_RU.md` залежно від обраної мови (EN / UK / RU).
- **Постпроцесинг TXT у Cursor:** прапорець «У Cursor», промпти в `redactor1.md`, API-ключ у `settings.json` / `CURSOR_API_KEY`; без ключа — відкриття Chat.

---

## Що нового в 1.1.0

- **Оновлення самої програми** з GitHub ([magnusua/WhisperFastGUI](https://github.com/magnusua/WhisperFastGUI)) через кнопку [Оновлення].
- **Оновлення ваг Whisper** (Hugging Face Hub) — у загальному процесі оновлень і окремою кнопкою «Оновити модель» у діалозі вибору моделі.
- **Коректне оновлення PyTorch** на системах з NVIDIA: перевірка версій лише з індексу **cu121**; модель відеокарти зберігається в `settings.json`.
- **Черга слідкування за каталогом:** якщо під час обробки з’являються нові файли, вони автоматично обробляються після завершення поточного завдання.

---

## Встановлення та налаштування

### Первинне встановлення

1. **Встановіть Python 3.9–3.13** (рекомендовано **3.12**). При встановленні позначте «Add Python to PATH».  
   Python 3.12.10 для Windows (64-bit): https://www.python.org/ftp/python/3.12.10/python-3.12.10-amd64.exe  
   Усі версії: https://www.python.org/downloads/

2. **Встановіть FFmpeg** і додайте його в PATH.  
   https://ffmpeg.org/download.html

3. **Запустіть `install.bat`.** Скрипт:
   - перевіряє Python та вже встановлені пакети;
   - оновлює pip, setuptools, wheel;
   - встановлює PyTorch (CUDA 12.1), faster-whisper, ctranslate2;
   - пропускає nvidia-cublas-cu12 та nvidia-cudnn-cu12 при першому встановленні (їх можна встановити з GUI: [Оновлення] або [Залежності]);
   - встановлює pygame, pydub, tkinterdnd2-universal, pystray, Pillow, cursor-sdk;
   - перевіряє пакети, FFmpeg та CUDA.

4. **Запустіть Whisper Fast GUI.** Якщо знайдено кілька версій Python, виберіть потрібну в діалозі першого запуску. Вибір зберігається у `settings.json`. Щоб повторити вибір, видаліть ключі `python_path` і `python_version` з цього файла.

5. **Запуск:**
   - `run_whisper.vbs` — подвійний клік (вікно консолі не показується).
   - Ярлик можна створити вручну: ціль — `run_whisper.vbs` або `main.py`.  
   Для відлагодження з CMD: `py main.py` (з папки проекту).

### Автоматичне встановлення

При першому запуску скрипт перевіряє та встановлює відсутні залежності (без компонентів NVIDIA). Бібліотеки NVIDIA встановлюються лише через [Оновлення] або [Залежності] в GUI.

### Встановлення вручну

У командному рядку в каталозі проекту виконайте: `python installer.py`.

---

## Залежності (таблиця)

Усі пакети, потрібні для роботи програми, та призначення кожного:

| Пакет | Призначення |
|-------|-------------|
| **pip** | Встановлення та оновлення пакетів Python. |
| **setuptools** | Збірка та встановлення пакетів. |
| **wheel** | Формат бінарних пакетів для швидкого встановлення. |
| **torch** | PyTorch — основа для роботи моделі Whisper на CPU або GPU. |
| **torchvision** | Додаткові компоненти PyTorch (потрібні разом із torch). |
| **torchaudio** | Робота з аудіо в PyTorch. |
| **faster-whisper** | Модель розпізнавання мовлення (обгортка над Whisper). |
| **ctranslate2** | Швидке інференс-ядро для моделей перетворення (використовується faster-whisper). |
| **nvidia-cublas-cu12** | Бібліотеки NVIDIA CUDA 12 для прискорення на GPU (встановлюються з GUI). |
| **nvidia-cudnn-cu12** | Бібліотеки cuDNN для NVIDIA GPU (встановлюються з GUI). |
| **pydub** | Обробка аудіо: витягування відрізків, експорт у MP3, отримання тривалості. |
| **pygame** | Відтворення звуку завершення обробки (звукове сповіщення). |
| **tkinterdnd2-universal** | Перетягування файлів (Drag & Drop) у вікно та всередині таблиці черги. |
| **pystray** | Іконка програми в системному треї (область сповіщень). |
| **Pillow** | Завантаження та підготовка зображення іконки для трею (favicon.ico). |
| **cursor-sdk** | Cursor SDK для автоматичного постпроцесингу TXT/MD (кнопка [Оновлення] / [Залежності]). |
| **markitdown** | Конвертація PDF/DOC/DOCX (та інших підтримуваних форматів) у Markdown. |
| **pyaudioop** | Заміна видаленого модуля `audioop` у Python 3.13+ (потрібен для pydub). |
| **ffmpeg** | Системна програма (не pip): декодування аудіо/відео, перевірка тривалості. Потрібно встановити окремо і додати в PATH. |
| **pandoc** | Системна програма (не pip, опційно): експорт Markdown → Word (.docx). Див. https://pandoc.org/installing.html |

**Примітки:**
- **nvidia-cublas-cu12** та **nvidia-cudnn-cu12** при першому запуску (install.bat або автоустановка) не ставляться; їх встановлюють кнопки [Оновлення] або [Залежності] в програмі.
- **pyaudioop** потрібен лише для Python 3.13+; за потреби встановлюється автоматично.
- **tkinter** — частина стандартної поставки Python; використовується для графічного інтерфейсу.
- **pandoc** потрібен лише для опції **MD → Word**; без нього транскрибація й Cursor працюють як раніше.

---

## Безпека та стабільність

- **Безпечне завершення:** при закритті застосунку модель вивантажується з пам’яті, очищується кеш CUDA.
- **Скасування операції:** миттєва зупинка з коректним завершенням поточного сегмента.
- **Обробка помилок:** логування та коректна реакція на збої. Якщо під час обробки виникає помилка типу «list index out of range», програма показує підказку: у файлі може **відсутня звукова доріжка** або звуку немає — перевірте файл (наприклад, відкрийте його в медіаплеєрі).
- **Одна задача одночасно:** у будь-який момент виконується лише одна транскрибація; повторний [Старт] або новий файл зі слідкування під час виконання не запускають другу задачу паралельно.
